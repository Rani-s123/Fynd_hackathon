# Port the API listens on.
PORT=3001

# Where the SQLite database file lives. Defaults to ./data/defect.db
# (relative to the server/ folder) if not set. Use :memory: only for
# throwaway testing -- it is wiped on every restart.
DB_PATH=./data/defect.db
