# Leave this unset (or blank) to run entirely against the in-memory mock API,
# with no backend needed -- useful for quick UI iteration.
#
# Set it to point at the real backend (see /server) to get real persistence,
# a real database, and the fixed rule engine:
VITE_API_URL=http://localhost:3001
